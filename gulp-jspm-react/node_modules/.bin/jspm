#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.0.16.53@jspm/jspm.js" "$@"
  ret=$?
else 
  node  "$basedir/../.0.16.53@jspm/jspm.js" "$@"
  ret=$?
fi
exit $ret
